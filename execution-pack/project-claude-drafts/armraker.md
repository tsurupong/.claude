# CLAUDE.md
## このプロジェクトについて
未確認。`/mnt/d/workspace/project/armraker` ディレクトリは存在するが中身が空であり、目的を示す情報が一切ない(README・マニフェスト・ソースファイルなし)。

## 技術スタック
情報なし。言語・フレームワーク・依存関係を示すファイル(package.json、Cargo.toml、pyproject.toml等)が存在しない。

## コマンド
- build: 未確認
- test: 未確認
- run: 未確認

いずれもマニフェストファイルが存在しないため確認不可能。

## 構造
ディレクトリは空。サブディレクトリ・ファイルは一切存在しない(`find`結果はディレクトリ自身のみ)。

## 規約・注意
情報なし。コードが存在しないため命名規則・構成慣習・生成物の有無について判断できる材料がない。

## 現在の状態
`.git` ディレクトリが存在しないため `git log` は実行不可。推測: プロジェクトディレクトリのみが作成された初期段階、またはリポジトリ本体が未クローン/未展開の状態と考えられる。作業を進めるには、まずソースコードの配置(clone/展開)またはプロジェクト初期化が必要と推測される。
